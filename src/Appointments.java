import javax.swing.*;
import java.awt.*;


public class Appointments extends JPanel {
    public Appointments() {
        this.setBackground(Color.WHITE);
        this.add(new JLabel("Appointments page"));
    }
}